
struct KSurfaceData {
	vec4 iAlbedo;
	vec3 iNormal; // tangent space normal, when tangent space lighting is being used
	float iEmissive;
	float iSpecular;
	float iOcclusion;
};

void vGetSurfaceData(inout KSurfaceData data);
